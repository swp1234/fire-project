# i18n 다국어 지원 가이드

> 모든 앱/게임은 반드시 12개 언어를 지원해야 함

## 지원 언어 (12개)

| 코드 | 언어 | 비고 |
|------|------|------|
| `ko` | 한국어 | 기본 |
| `en` | English | 글로벌 기본 |
| `zh` | 中文 | 중국어 간체 |
| `hi` | हिन्दी | 힌디어 |
| `ru` | Русский | 러시아어 |
| `ja` | 日本語 | 높은 RPM |
| `es` | Español | 5억+ 화자 |
| `pt` | Português | 브라질 |
| `id` | Bahasa Indonesia | 2.7억 |
| `tr` | Türkçe | 터키어 |
| `de` | Deutsch | 높은 RPM |
| `fr` | Français | 프랑스어 |

## 파일 구조

```
project/
├── js/
│   ├── i18n.js          # I18n 클래스
│   └── locales/
│       ├── ko.json
│       ├── en.json
│       ├── zh.json
│       ├── hi.json
│       ├── ru.json
│       ├── ja.json
│       ├── es.json
│       ├── pt.json
│       ├── id.json
│       ├── tr.json
│       ├── de.json
│       └── fr.json
├── index.html           # data-i18n 속성 + 언어 선택기
```

## I18n 클래스 표준

```javascript
class I18n {
    constructor() {
        this.translations = {};
        this.supportedLanguages = ['ko','en','zh','hi','ru','ja','es','pt','id','tr','de','fr'];
        this.currentLang = this.detectLanguage();
    }
    detectLanguage() { /* localStorage → browser → 'en' */ }
    async loadTranslations(lang) { /* fetch locales/{lang}.json */ }
    t(key) { /* dot notation: 'app.title' → translations.app.title */ }
    async setLanguage(lang) { /* 언어 변경 + UI 갱신 */ }
    updateUI() { /* data-i18n 속성 자동 번역 */ }
    getCurrentLanguage() { return this.currentLang; }
    getLanguageName(lang) { /* 언어명 반환 */ }
}
```

## HTML 언어 선택기

```html
<div class="language-selector">
    <button id="lang-toggle">🌐</button>
    <div id="lang-menu" class="lang-menu hidden">
        <button class="lang-option" data-lang="ko">🇰🇷 한국어</button>
        <button class="lang-option" data-lang="en">🇺🇸 English</button>
        <button class="lang-option" data-lang="zh">🇨🇳 中文</button>
        <button class="lang-option" data-lang="hi">🇮🇳 हिन्दी</button>
        <button class="lang-option" data-lang="ru">🇷🇺 Русский</button>
        <button class="lang-option" data-lang="ja">🇯🇵 日本語</button>
        <button class="lang-option" data-lang="es">🇪🇸 Español</button>
        <button class="lang-option" data-lang="pt">🇧🇷 Português</button>
        <button class="lang-option" data-lang="id">🇮🇩 Indonesia</button>
        <button class="lang-option" data-lang="tr">🇹🇷 Türkçe</button>
        <button class="lang-option" data-lang="de">🇩🇪 Deutsch</button>
        <button class="lang-option" data-lang="fr">🇫🇷 Français</button>
    </div>
</div>
```

## IIFE 안전 패턴 (CRITICAL)

```javascript
// 반드시 try-catch로 감싸기 - loader 멈춤 방지
(async function initI18n() {
    try {
        await i18n.loadTranslations(i18n.currentLang);
        i18n.updateUI();
    } catch (e) {
        console.warn('i18n load failed:', e);
    } finally {
        // loader는 반드시 숨기기
        const loader = document.getElementById('app-loader');
        if (loader) {
            loader.classList.add('hidden');
            setTimeout(() => loader.remove(), 300);
        }
    }
})();
```

## 번역 품질 규칙

- 자연스러운 현지어 (일본어 です/ます, 독일어 Sie, 프랑스어 vous)
- 앱별 도메인 용어 정확 번역
- JSON 키 구조 모든 언어 동일
- 새 기능 시 12개 언어 JSON 모두 업데이트

## 인코딩 안전 규칙 (CRITICAL)

- locale JSON은 반드시 `UTF-8`로 유지한다.
- Windows/PowerShell에서 `node -e`, here-string, one-liner로 locale JSON을 대량 수정할 때는 비ASCII 문자열을 직접 커맨드 본문에 넣지 않는다.
- 다국어 bulk edit가 필요하면 다음 둘 중 하나만 사용한다.
  - 저장된 스크립트 파일에서 `UTF-8`로 읽고 쓰기
  - ASCII-safe payload(`\\uXXXX` escape 등)로 JSON 키만 갱신
- locale 수정 직후에는 최소 `ko`, `ja`, `zh`, `ru` 4개 언어를 다시 열어 `?`, `�`, 깨진 구두점이 없는지 확인한다.
- 배포 전 검증은 최소 다음 순서를 지킨다.
  1. locale JSON parse 확인
  2. `git diff --check`
  3. 로컬 렌더링 또는 DOM 텍스트 확인
  4. 라이브 locale JSON endpoint 확인
  5. 라이브 실제 페이지 렌더링 확인
- 번역 사고가 발생하면 핫픽스만으로 끝내지 말고, 같은 세션에 이 문서나 운영 문서에 재발 방지 규칙을 추가한다.

## 블로그 다국어

- `portal/blog/{lang}/` 폴더
- 각 언어별 최소 3개 SEO 글
- Schema.org, OG태그, hreflang 태그 필수

## 적용 타이밍

| 시점 | 적용 |
|------|------|
| 새 앱/게임 개발 | 개발 직후 즉시 |
| 기존 앱 수정 | 모든 locale 동시 업데이트 |
| Google Play 출시 | i18n 미적용 시 출시 불가 |
## Portal Hub Locale Regression Guard (2026-04-22)

- Bug type 1: missing locale keys leaked raw key strings such as `hub_games.feat1_tag` into the live UI.
- Bug type 2: new follow-up rails were shipped with hardcoded English copy and no `data-i18n`, `data-i18n-html`, or `data-app` binding.
- Bug type 3: a page added `data-i18n-html` markup but the page-level i18n script only rendered `[data-i18n]`, so rich localized headings stayed in fallback text.

Required guardrails for `projects/portal` hub pages:

1. Every newly added visible string must be wired through `data-i18n`, `data-i18n-html`, or a `data-app` container that resolves copy from `APP_DATA`.
2. If a page uses `data-i18n-html`, its page-level initialization must explicitly render both `[data-i18n]` and `[data-i18n-html]`.
3. Any new locale key used by a hub page must exist in all 12 portal locale JSON files before deploy.
4. Before pushing `projects/portal`, run `node scripts/portal-hub-locale-audit.js`.
5. The portal quality gate must stay green with the portal hub locale audit enabled; do not bypass that check for hub-page copy changes.

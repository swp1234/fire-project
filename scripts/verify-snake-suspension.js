#!/usr/bin/env node

const fs = require('fs');
const http = require('http');
const path = require('path');
const { chromium } = require('playwright');
const { listenOnSafePort } = require('./lib/safe-local-port');

const ROOT = path.resolve(__dirname, '..');
const APP = path.join(ROOT, 'projects', 'snake-game');
const PORTAL = path.join(ROOT, 'projects', 'portal');
const LOCALES = ['ko', 'en', 'zh', 'hi', 'ru', 'ja', 'es', 'pt', 'id', 'tr', 'de', 'fr'];
const EVENTS = ['snake_view', 'snake_start', 'snake_progress', 'snake_complete', 'snake_share', 'snake_related_click'];
const RELATED = ['/flappy-bird/', '/zigzag-runner/', '/maze-runner/', '/stack-tower/'];
function assert(value, message) { if (!value) throw new Error(message); }
function read(relative) { return fs.readFileSync(path.join(APP, relative), 'utf8'); }
function count(text, regex) { return Array.from(text.matchAll(regex)).length; }

function fixture(overrides = {}) {
  return { html: overrides.html ?? read('index.html'), css: overrides.css ?? read('css/style.css'), app: overrides.app ?? read('js/app.js'), i18n: overrides.i18n ?? read('js/i18n.js'), sw: overrides.sw ?? read('sw.js'), manifest: overrides.manifest ?? read('manifest.json'), readme: overrides.readme ?? read('README.md'), locales: overrides.locales ?? Object.fromEntries(LOCALES.map((lang) => [lang, read(`js/locales/${lang}.json`)])) };
}

function verifySource(overrides = {}) {
  const value = fixture(overrides); const source = [value.html, value.css, value.app, value.i18n, value.sw, ...Object.values(value.locales)].join('\n');
  assert(/data-ad-serving="suspended-invalid-traffic-2026-09-03"/.test(value.html), 'Snake suspension marker missing');
  assert(!/pagead2|adsbygoogle|data-ad-slot|\/portal\/js\/game-ads\.js|\bGameAds\b/i.test(source), 'Active ad or game-ad code conflicts with suspension');
  assert(!/showInterstitial|showRewarded|rewarded|Watch Ad|ad-banner|ad-placeholder|interstitial-card|btn-revive/i.test(source), 'Interstitial, reward, revive, or fake ad surface remains');
  assert(!/aggregateRating|ratingCount|FAQPage|page_engage|traffic_quality|content_ad_impression/i.test(value.html + value.app), 'Fabricated proof, hidden FAQ, or synthetic telemetry remains');
  assert(!/cross-promo|DailyStreak|GameAchievements|recommendations-section/i.test(source), 'Unqualified retention or generic promotion remains');
  assert(!/user-scalable\s*=\s*no/i.test(value.html), 'Viewport disables user zoom');
  for (const event of EVENTS) assert(count(value.app, new RegExp(`trackSnakeStage\\('${event}'\\)`, 'g')) === 1, `${event} call drifted`);
  assert(/const snakeStages = new Set\(\)/.test(value.app) && /snakeStages\.has\(name\)/.test(value.app), 'Exact-once funnel guard missing');
  assert(/beginRun\(\)[\s\S]*gameState === 'playing'[\s\S]*trackSnakeStage\('snake_start'\)/.test(value.app), 'Start must require board activation');
  assert(/moves === 1[^\n]+snake_progress/.test(value.app) && value.app.indexOf("trackSnakeStage('snake_progress')") > value.app.indexOf('this.snake.unshift'), 'Progress must require a completed grid movement');
  assert(/showGameOver\(\);\s*trackSnakeStage\('snake_complete'\)/.test(value.app), 'Completion must follow game over');
  assert(!/trackSnakeStage\('snake_(?:view|start|progress|complete|share|related_click)'\s*,/.test(value.app), 'Private game value entered telemetry');
  assert(/async shareScore\(\)/.test(value.app) && /await navigator\.share/.test(value.app) && /await navigator\.clipboard\.writeText/.test(value.app), 'Share success is not awaited');
  assert(!/\?score=|shareTemplate|share_msg/.test(value.app + value.html), 'Score-bearing share path remains');
  assert(/params\.get\('lang'\)/.test(value.i18n) && value.i18n.indexOf("params.get('lang')") < value.i18n.indexOf("localStorage.getItem('i18n_lang')"), 'URL language no longer has first priority');
  const related = Array.from(value.html.matchAll(/<a href="([^"]+)" class="related-card"/g), (match) => match[1]);
  assert(JSON.stringify(related) === JSON.stringify(RELATED), `Related route contract drifted: ${related.join(', ')}`);
  assert(/event\.target\.closest\('\.related-card'\)/.test(value.app), 'Nested related click is not attributed');
  assert(/CACHE_NAME = 'snake-classic-v6'/.test(value.sw) && /name\.startsWith\(CACHE_PREFIX\)/.test(value.sw), 'Service-worker cache boundary drifted');
  assert(/event\.request\.method !== 'GET'/.test(value.sw) && /url\.origin !== self\.location\.origin/.test(value.sw) && /url\.pathname\.startsWith\(APP_PATH\)/.test(value.sw) && /response\.ok/.test(value.sw), 'Service-worker request boundary drifted');
  assert(!/['"]\/snake-game\//.test(value.sw.match(/const ASSETS_TO_CACHE = \[([\s\S]*?)\];/)?.[1] || ''), 'Service-worker assets must be relative');
  const manifest = JSON.parse(value.manifest); assert(manifest.scope === './' && manifest.start_url === './' && !manifest.share_target, 'Manifest must remain deployment-relative and truthful');
  for (const [lang, text] of Object.entries(value.locales)) { const locale = JSON.parse(text); assert(!locale.ad && !locale.share_msg && !locale.gameover?.revive, `${lang} locale retired ad/share copy returned`); }
  assert(Buffer.byteLength(value.readme, 'utf8') < 1800 && /Opening menus, selecting a mode/i.test(value.readme) && /invalid-traffic restriction/i.test(value.readme), 'README is stale or oversized');
  return { locales: LOCALES.length, events: EVENTS.length, related: related.length };
}

async function startServer() {
  const types = { '.css':'text/css','.html':'text/html','.js':'application/javascript','.json':'application/json','.jpg':'image/jpeg','.png':'image/png','.svg':'image/svg+xml' };
  const server = http.createServer((request, response) => { try { const pathname = decodeURIComponent(new URL(request.url, 'http://127.0.0.1').pathname); let base, relative; if (pathname.startsWith('/snake-game/')) { base=APP; relative=pathname.slice(12)||'index.html'; } else if (pathname.startsWith('/portal/')) { base=PORTAL; relative=pathname.slice(8); } else return response.writeHead(404).end(); let target=path.resolve(base,relative); assert(target===base||target.startsWith(`${base}${path.sep}`),`Unsafe path: ${pathname}`); if(fs.existsSync(target)&&fs.statSync(target).isDirectory())target=path.join(target,'index.html'); if(!fs.existsSync(target)||!fs.statSync(target).isFile())return response.writeHead(404).end(); response.writeHead(200,{'Cache-Control':'no-store','Content-Type':`${types[path.extname(target)]||'application/octet-stream'}; charset=utf-8`}); response.end(fs.readFileSync(target)); } catch(error){response.writeHead(400).end(error.message);} });
  const address=await listenOnSafePort(server); return {origin:`http://127.0.0.1:${address.port}`,close:()=>new Promise(resolve=>server.close(resolve))};
}
function funnelEvents(rows) { return rows.filter(row=>row?.[0]==='event'&&EVENTS.includes(row[1])).map(row=>({name:row[1],params:row[2]||{}})); }

async function verifyRuntime(baseUrl) {
  const browser=await chromium.launch({headless:true});
  try { for(const viewport of [{width:390,height:844},{width:1440,height:900}]) { const context=await browser.newContext({viewport}); const page=await context.newPage(); const errors=[]; page.on('pageerror',e=>errors.push(e.message)); await page.route('**/*',route=>new URL(route.request().url()).origin===new URL(baseUrl).origin?route.continue():route.abort()); await page.addInitScript(()=>{localStorage.setItem('i18n_lang','ko');Object.defineProperty(navigator,'share',{configurable:true,value:async()=>true});}); await page.goto(`${baseUrl}/snake-game/?lang=en`,{waitUntil:'domcontentloaded',timeout:20000}); await page.waitForFunction(()=>window.game&&window.i18n?.currentLang==='en',null,{timeout:15000}); assert(await page.evaluate(()=>!window.dataLayer?.some(row=>row?.[1]==='snake_start')),`${viewport.width}px load counted as start`); await page.locator('#btn-start').click(); await page.locator('#mode-wall').click(); assert(await page.evaluate(()=>!window.dataLayer?.some(row=>row?.[1]==='snake_start')),`${viewport.width}px mode choice counted as start`); await page.locator('#game-canvas').click({position:{x:20,y:20}}); await page.waitForFunction(()=>window.dataLayer?.some(row=>row?.[1]==='snake_start')); await page.waitForFunction(()=>window.dataLayer?.some(row=>row?.[1]==='snake_progress')); await page.evaluate(()=>window.game.endGame()); await page.waitForFunction(()=>!document.getElementById('gameover-screen').classList.contains('hidden')); await page.locator('#btn-share').click(); await page.waitForFunction(()=>window.dataLayer?.some(row=>row?.[1]==='snake_share')); await page.evaluate(()=>document.querySelector('.related-grid').addEventListener('click',event=>event.preventDefault())); await page.locator('.related-card span[data-i18n]').first().click(); await page.waitForFunction(()=>window.dataLayer?.some(row=>row?.[1]==='snake_related_click')); const report=await page.evaluate(()=>({events:(window.dataLayer||[]).filter(row=>row?.[0]==='event'),overflow:Math.max(document.documentElement.scrollWidth,document.body.scrollWidth)-innerWidth,targets:['btn-retry','btn-share','btn-menu'].map(id=>{const r=document.getElementById(id).getBoundingClientRect();return{id,width:r.width,height:r.height};}),related:Array.from(document.querySelectorAll('.related-card'),link=>{const r=link.getBoundingClientRect();return{href:link.getAttribute('href'),width:r.width,height:r.height};}),adSurface:document.querySelectorAll('script[src*="pagead2"],script[src*="game-ads"],ins.adsbygoogle,[data-ad-slot],[class*="ad-banner"],[id*="interstitial"]').length})); const stages=funnelEvents(report.events); for(const name of EVENTS)assert(stages.filter(e=>e.name===name).length===1,`${viewport.width}px ${name} must fire exactly once`); assert(stages.every(e=>!Object.keys(e.params).some(k=>/score|rank|mode|food|direction|time|duration|result|url|path|language/i.test(k))),`${viewport.width}px private value entered telemetry`); assert(report.overflow<=0&&[...report.targets,...report.related].every(t=>t.width>=43.99&&t.height>=43.99),`${viewport.width}px layout or touch target regressed: ${JSON.stringify(report)}`); assert(JSON.stringify(report.related.map(x=>x.href))===JSON.stringify(RELATED),`${viewport.width}px related routes drifted`); assert(report.adSurface===0&&errors.length===0,`${viewport.width}px ad surface or runtime error: ${errors.join(' | ')}`); await context.close(); } } finally { await browser.close(); }
}

function verifyMutations() { const base=fixture(); const cases=[['marker',{html:base.html.replace('data-ad-serving="suspended-invalid-traffic-2026-09-03"','')}],['loader',{html:base.html.replace('</head>','<script src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js"></script></head>')}],['reward',{app:`${base.app}\nGameAds.showRewarded();`}],['rating',{html:base.html.replace('</body>','<div>aggregateRating</div></body>')}],['faq',{html:base.html.replace('</body>','<div>FAQPage</div></body>')}],['synthetic',{app:`${base.app}\ngtag('event','page_engage');`}],['start',{app:base.app.replace("trackSnakeStage('snake_start');",'')}],['progress',{app:base.app.replace('this.moves === 1','this.moves === 0')}],['private',{app:base.app.replace("trackSnakeStage('snake_progress');","trackSnakeStage('snake_progress', this.score);")}],['exact-once',{app:base.app.replace('snakeStages.has(name)','false')}],['share',{app:base.app.replace('await navigator.share','navigator.share')}],['language',{i18n:base.i18n.replace("const urlLang = params.get('lang');","const urlLang = '';")}],['nested',{app:base.app.replace("event.target.closest('.related-card')","event.target.matches('.related-card')")}],['route',{html:base.html.replace('href="/flappy-bird/"','href="/unknown/"')}],['zoom',{html:base.html.replace('initial-scale=1.0','initial-scale=1.0, user-scalable=no')}],['root-cache',{sw:base.sw.replace("'./index.html'","'/snake-game/index.html'")}],['global-cache',{sw:base.sw.replace('name.startsWith(CACHE_PREFIX) && ','')}],['scope',{sw:base.sw.replace(' || !url.pathname.startsWith(APP_PATH)','')}],['success',{sw:base.sw.replace('if (response.ok)','if (response)')}],['manifest',{manifest:base.manifest.replace('"scope": "./"','"scope": "/"')}],['locale',{locales:{...base.locales,en:base.locales.en.replace(/^\{/,'{\n  "ad": {},')}}],['readme',{readme:base.readme+'x'.repeat(1800)}]]; for(const [name,override] of cases){let detected=false;try{verifySource({...base,...override});}catch(error){detected=true;console.log(`[PASS] ${name}: ${error.message}`);}assert(detected,`Mutation escaped: ${name}`);}console.log(`Mutation summary: ${cases.length}/${cases.length} detected`); }
async function main(){const at=process.argv.indexOf('--url');const production=at>=0?process.argv[at+1].replace(/\/$/,''):'';const result=verifySource();if(process.argv.includes('--mutations'))verifyMutations();if(production)await verifyRuntime(production);else{const server=await startServer();try{await verifyRuntime(server.origin);}finally{await server.close();}}console.log(`[PASS] Snake suspension: ${result.locales} locales, ${result.related} related routes, ${result.events} exact-once stages`);} main().catch(error=>{console.error(`[FAIL] ${error.stack||error.message}`);process.exitCode=1;});
